<h1>Customers</h1>

<ul>
  <% @customers.each do |customer| %>
    <li>
			<%= customer.name %>
	  	<%= customer.location %>
	  	<%= customer.number %>
    </li>
  <% end %>
</ul>

<%= link_to "New Customer", new_customer_path %>